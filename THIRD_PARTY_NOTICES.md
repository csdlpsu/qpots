# Third-party notices

qPOTS depends on separately distributed Python libraries whose licenses are
reported by their respective package metadata. The qPOTS source and binary
distributions do not contain the MATLAB implementation of TS-EMO.

`TSEMORunner` is an optional interoperability layer for an external TS-EMO
checkout. Users must obtain that software independently from its upstream
authors and confirm that their intended use complies with the upstream terms.
Passing `tsemo_path` to qPOTS does not copy, modify, or redistribute the
external checkout.
